## How start project
* clone repository
* install nodejs
* `npm install`

## Quick start
* `npm run test` - Runs all tests
* `npm run test_single` - Runs one stream

## Reports
* `npm run generate_report`